package js.html;

@:native("Navigator")
extern class Navigator
{
	// public var mediaDevices(get, never):js.html.MediaDevices;
	// inline function get_mediaDevices() return untyped __js__("mediaDevices");

	// function mediaDevices() : MediaDevices;
	var mediaDevices(default, null) : MediaDevices;

	// public static var mediaDevices(get, never):js.html.MediaDevices;
	// function get_mediaDevices():js.html.MediaDevices {
	// 	return return untyped __js__("mediaDevices");
	// }

}